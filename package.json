{
  "name": "wishmade-root",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "admin-panel"
  ],
  "scripts": {
    "install:all": "npm install",
    "build:frontend": "CI=false npm run build --workspace=frontend",
    "build:admin": "CI=false npm run build --workspace=admin-panel"
  },
  "dependencies": {
    "axios": "^1.13.2"
  }
}